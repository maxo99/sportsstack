apiVersion: v1
kind: Service
metadata:
  name: go-sportsagent
  namespace: sportsstack
spec:
  type: ClusterIP
  selector:
    app: go-sportsagent
  ports:
    - port: 8082
      targetPort: 8082
      name: http
