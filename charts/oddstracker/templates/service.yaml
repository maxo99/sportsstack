apiVersion: v1
kind: Service
metadata:
  name: oddstracker
  namespace: sportsstack
spec:
  type: ClusterIP
  selector:
    app: oddstracker
  ports:
    - port: 8080
      targetPort: 8080
      name: http
