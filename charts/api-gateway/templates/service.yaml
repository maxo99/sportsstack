apiVersion: v1
kind: Service
metadata:
    name: api-gateway
    namespace: sportsstack
spec:
    type: ClusterIP
    selector:
        app: api-gateway
    ports:
      - port: 8088
        targetPort: 8088
        name: http
