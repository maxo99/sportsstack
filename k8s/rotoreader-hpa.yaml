apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rotoreader
  namespace: sportsstack
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rotoreader
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
